{
  "name": "@dcgauld/bloom",
  "version": "0.1.0",
  "description": "A tiny CSS library for web projects.",
  "license": "MIT",
  "repository": "dcgauld/bloom",
  "main": "bloom.css",
  "scripts": {
    "build": "node scripts/build.js",
    "postinstall": "husky install",
    "postpublish": "pinst --enable",
    "prepublishOnly": "pinst --disable",
    "prettier": "prettier --write ."
  },
  "devDependencies": {
    "@commitlint/cli": "^12.0.1",
    "@commitlint/config-conventional": "^12.0.1",
    "autoprefixer": "^10.2.1",
    "cssnano": "^4.1.10",
    "fs-extra": "^9.0.1",
    "husky": "^6.0.0",
    "pinst": "^2.1.1",
    "postcss": "^8.2.4",
    "postcss-import": "^14.0.0",
    "prettier": "^2.2.1"
  }
}
